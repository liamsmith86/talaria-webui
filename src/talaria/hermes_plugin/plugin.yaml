name: talaria
version: "1.2.0"
description: Profile instructions, ephemeral prefill, response details, and native session controls for Talaria.
provides_hooks:
  - pre_api_request
  - post_api_request
  - on_session_end
