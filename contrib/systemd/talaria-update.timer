# Opt in with: systemctl --user enable --now talaria-update.timer
[Unit]
Description=Check and install Talaria updates daily

[Timer]
OnCalendar=daily
RandomizedDelaySec=30min
Persistent=true
Unit=talaria-update.service

[Install]
WantedBy=timers.target
